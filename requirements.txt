# Phase 1 (environment) and Phase 2 (agent) need only the Python stdlib.
# The LLM planner calls Groq's OpenAI-compatible API over urllib — no SDK.
# These arrive in Phase 4 (demo UI):
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
